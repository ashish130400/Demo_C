#include <iostream>
#include <fstream>
#include <string>

using namespace std;

void fucntion(ofstream &out)
{

}

int main()
{
	
	/*ofstream out;
	out.open("records.txt", ios::out);

	if (out.is_open())
	{
		out << "sachin" << " " << 100 << endl;
	}
	else
	{
		cout << "unable to crrate";
	}
	out.close();



	ifstream iff;
	iff.open("records.txt", ios::in);
	string player, country;
	int runs;
	if (iff.is_open())
	{
		while (!iff.eof())
		{
			iff >> player >> runs >> country;
			cout << player << "" << runs << "" << country << endl;
			if (iff.eof())
				break;
			
		}
	}
	else
	{
		cout << "unable to read";
	}

	iff.close();

*/

	return 0;
}